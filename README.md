# ipl

Ball-by-ball IPL JSON (Cricsheet) through import, plots, and simple models.

```bash
make setup
make all
```

`make ingest` · `make visuals` · `make model`  

Data notes: `README.txt` · pipeline: `docs/ARCHITECTURE.md`
